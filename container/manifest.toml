# SPDX-License-Identifier: PMPL-1.0-or-later
#
# Cerro Torre manifest for cloudguard-cli .ctp bundle
#
# This manifest describes the container image for verified
# container packaging. Used by `ct pack` to create .ctp bundles.

[metadata]
name = "cloudguard-cli"
version = "0.1.0"
revision = 1
summary = "CloudGuard CLI — standalone Cloudflare domain security management tool"
description = """
cloudguard-cli — containerised service packaged as a verified
cerro-torre .ctp bundle with Ed25519 signing and full provenance
tracking.
"""
license = "{{LICENSE}}"
homepage = "https://github.com/hyperpolymath/cloudguard-cli"
maintainer = "Jonathan D.A. Jewell <{{EMAIL}}>"

[provenance]
upstream = "https://github.com/hyperpolymath/cloudguard-cli"
import_date = 2026-03-02T00:00:00Z

[dependencies]
runtime = ["ca-certificates", "curl"]
build = []

[build]
system = "podman"

[build.environment]
APP_HOST = "[::]"
APP_PORT = "3846"

[outputs]
primary = "cloudguard-cli"
split = []

[attestations]
require = ["source-signature", "sbom-complete"]
recommend = ["security-audit", "reproducible-build"]

# Runtime security profile
[security]
user = "appuser"
group = "appuser"
read_only_root = false
no_new_privileges = true

[security.capabilities]
drop = ["ALL"]
add = ["NET_BIND_SERVICE"]

[security.network]
listen_tcp = [3846]

[security.filesystem]
read = ["/app/", "/data/"]
write = ["/data/", "/tmp/"]
execute = ["/app/entrypoint.sh"]
